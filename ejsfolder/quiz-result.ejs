<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Result</title>
    <link rel="stylesheet" href="/static/css/bootstrap.min.css"> <!-- Adjust path as needed -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <!-- For icons -->
    <style>
        body {
            background-color: #f8f9fa;
        }

        .container {
            margin-top: 30px;
        }

        .quiz-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .quiz-header h1 {
            font-size: 2.5rem;
            color: #343a40;
        }

        .result-summary {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
        }

        .result-summary p {
            font-size: 1.25rem;
            margin: 10px 0;
        }

        .question-card {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        .question-card hr {
            margin: 20px 0;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            width: 100%;
            margin-top: 20px;
        }

        @media (min-width: 768px) {
            .btn-primary {
                width: auto;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="quiz-header">
            <h1 class='text-success'><i class="fas fa-check-circle text-success"></i> Quiz Result</h1>
        </div>
        <div class="result-summary">
            <p><strong>Your total score:</strong> <%= totalMarks %></p>
            <p><strong>Duration:</strong> <%= duration %> seconds</p>
        </div>

        <h2>Your Answers:</h2>
        <div class="row">
            <% studentAnswers.forEach(answer => { %>
            <div class="col-12 col-md-6">
                <div class="question-card">
                    <p><strong>Question:</strong> <%= answer.question_text %></p>
                    <p><strong>Your Answer:</strong> <%= answer.selected_choice_text %>
                        <span
                            class="<%= answer.selected_choice_id === answer.correct_choice_id ? 'text-success' : 'text-danger' %>">
                            (<%= answer.selected_choice_id === answer.correct_choice_id ? 'Correct' : 'Incorrect' %>)
                        </span>
                    </p>
                    <% if (answer.selected_choice_id !== answer.correct_choice_id) { %>
                    <p><strong>Correct Answer:</strong> <%= answer.correct_choice_text %></p>
                    <% } %>
                    <p><strong>Received Mark:</strong> <%= answer.marks_awarded %></p>
                </div>
            </div>
            <% }) %>
        </div>

        <a href="/studenthome" class="btn btn-primary"><i class="fas fa-arrow-left"></i> Back to Quiz</a>
    </div>

    <script src="/static/js/bootstrap.bundle.min.js"></script> <!-- Adjust path as needed -->
</body>

</html>