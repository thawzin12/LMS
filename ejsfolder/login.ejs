<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Y School</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

    <link rel="stylesheet" href="/static/css/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/static/css/login.css">
    <!-- <link rel="stylesheet" href="/static/css/mainstyle.css"> -->
    <link rel="icon" href="logo.png">
</head>

<body class="body-login">
    <div class="circle"></div>
    <div class="d-flex justify-content-center align-items-center min-vh-100">
        <form class="login needs-validation box" method="post" action="/login" novalidate>
            <div class="text-center mb-4">
                <!-- <img src="/static/images/logo.avif" width="100" alt="Logo"
                    class="animate__animated animate__fadeInDown"> -->
            </div>
            <h3 class="text-success text-center font-weight-bold lob animate__animated animate__fadeInUp">LOGIN</h3>

            <div class="mb-3 animate__animated animate__fadeIn">
                <label for="email" class="form-label text-dark">Email</label>
                <input type="email" class="form-control <%= errors.email ? 'is-invalid' : '' %>" id="email" name="email"
                    maxlength="50" value="<%= data.email || '' %>" required>
                <div class="invalid-feedback">
                    <%= errors.email || 'Email is required and must be a valid email address' %>
                </div>
            </div>
            <div class="mb-3 animate__animated animate__fadeIn">
                <label for="password" class="form-label text-dark">Password</label>
                <div class="input-group">
                    <input type="password" class="form-control <%= errors.password ? 'is-invalid' : '' %>" id="password"
                        name="password" minlength="8" maxlength="15" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"
                        value="<%= data.password || '' %>" required>
                    <button class="btn btn-success" type="button" id="togglePassword">
                        <i class="bi bi-eye-slash"></i>
                    </button>
                    <div class="invalid-feedback">
                        <%= errors.password || 'Password must be 8-15 characters, including at least one uppercase, one lowercase, and one digit' %>
                    </div>
                </div>
            </div>

            <div class="mb-3 animate__animated animate__fadeIn">
                <label class="form-label">Login As</label>
                <select class="form-select <%= errors.role ? 'is-invalid' : '' %>" name="role" required>
                    <option value="">Select Role</option>
                    <option value="admin">Admin</option>
                    <option value="teacher">Teacher</option>
                    <option value="student">Student</option>
                </select>
                <div class="invalid-feedback">
                    <%= errors.role || 'Please select a role' %>
                </div>
            </div>

            <div class="mb-3 form-check animate__animated animate__fadeIn">
                <input type="checkbox" class="form-check-input" id="exampleCheck1" required>
                <label class="form-check-label text-dark" style="font-weight: bolder;" for="exampleCheck1">Check me
                    out</label>
                <div class="invalid-feedback">
                    You must check this box before submitting.
                </div>
            </div>

            <div class="d-flex justify-content-start mb-3 animate__animated animate__fadeIn">
                <button type="submit" class="btn btn-success rounded-0 me-2 w-100">Login</button>
                <button type="reset" class="btn btn-dark rounded-0 w-100 ms-2">Reset</button>
            </div>

            <div class="row mt-3 animate__animated animate__fadeIn">
                <div class="col-md-6">
                    <a href="/" role="button" class="btn btn-primary w-100 rounded-0">Home</a>
                </div>
                <div class="col-md-6">
                    <a href="forgetpassword" role="button" class="btn btn-outline-primary w-100 rounded-0">Forgot
                        Password</a>
                </div>
            </div>

            <div class="text-center text-light mt-4 animate__animated animate__fadeIn">
                Copyright &copy; 2023 Magway CU. All rights reserved.
            </div>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        (function () {
            'use strict';
            var forms = document.querySelectorAll('.needs-validation');

            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault();
                            event.stopPropagation();
                        }
                        form.classList.add('was-validated');
                    }, false);
                });
        })();


    </script>
    <script src="/static/js/eye.js"></script>
</body>

</html>